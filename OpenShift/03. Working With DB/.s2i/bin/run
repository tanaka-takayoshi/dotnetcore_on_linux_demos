#!/bin/bash

set -e

echo "---> Running data migration..."
if [ -z "$ASPNETCORE_ENVIRONMENT" ]; then
   ASPNETCORE_ENVIRONMENT="Development"
fi

dotnet ef database update -e ${ASPNETCORE_ENVIRONMENT}

echo "---> Running application ..."
dotnet run